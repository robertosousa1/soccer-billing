# As variáveis de ambiente reais vivem em cada app, não na raiz:
#   apps/api/.env        (copie de apps/api/.env.example)
#   apps/web/.env.local   (copie de apps/web/.env.example)
#
# Este arquivo existe só como ponteiro — não é lido por nenhum processo.
